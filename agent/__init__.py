"""Agent layer: model-agnostic orchestrator + subagents over the tool registry.

See docs/AGENTS.md for the architecture. Entry point:
    python -m agent.orchestrator --task tasks/<task>.md --dry-run
"""
